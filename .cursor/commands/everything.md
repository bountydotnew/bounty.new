# Everything tag

Used to tag every relevant file in the codebase. 

To be used in parallel with commands the user wants to run on every file

EG:
/deslop /everything:
runs deslop on every necessary file (up to LLM discretion)